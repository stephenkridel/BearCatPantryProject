<div class="main-container">
    <h2>Active Orders</h2>
    <table class="table table-hover cart-table">
        <thead>
            <tr>
                <th scope="col">User</th>
                <th scope="col">Cart Contents</th>
                <th scope="col">Date created</th>
            </tr>
        </thead>
        <tbody>
            {{#each carts}}
            <tr>
                <th scope="row" class="first-row">{{this.user}}</th>
                <th scope="row" class="first-row">
                    {{#each this.items }}
                    {{this.itemName}} - {{this.quantity}}
                    <br>
                    {{/each}}
                </th>
                <th scope="row" class="first-row">{{this.lastModDate}}</th>
            </tr>
            {{/each}}
        </tbody>
    </table>
</div>